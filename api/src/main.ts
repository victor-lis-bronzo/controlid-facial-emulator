/**
 * Control-iD Facial Emulator — API entrypoint (placeholder).
 *
 * This is scaffolding only. The Bootstrap / StateMode selector, the Fastify
 * composition root, and route registration will be wired here in later tasks.
 * For now it simply logs a startup message so `tsc` has a compilable entrypoint
 * and `tsx watch src/main.ts` runs.
 */
function main(): void {
  console.log('[controlid-facial-emulator] api starting (placeholder entrypoint)…');
}

main();
